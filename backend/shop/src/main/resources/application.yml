server:
  # Fixed syntax error here
  port: 8080

spring:
  datasource:
    # Hardcoded Neon URL (added jdbc: prefix)
    url: jdbc:postgresql://ep-autumn-surf-agp3377v-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_ay0qkWNSwim8
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Changed to 'update' so it creates tables automatically
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false # Disable Flyway temporarily if you want Hibernate to manage tables
    locations: classpath:db/migration
    baseline-on-migrate: true

  data:
    redis:
      host: localhost
      port: 6379
      ssl:
        enabled: false

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
  info:
    title: FullStack Shop API
    version: 1.0.0
    description: JWT based shop

jwt:
  # Hardcoded secret for local dev
  secret: secret256BitKeyPentruDev123456789012345678901234567890
  expiration: 3600

roles:
  admin: ADMIN
  customer: CUSTOMER

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG